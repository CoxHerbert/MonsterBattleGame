export class Collision {
  resolveCircleObstacles(circle, obstacles = []) {
    // TODO: implement collision resolution
  }
  pointHitObstacle(x, y, obstacles = []) {
    // TODO: return whether point hits any obstacle
    return false;
  }
}
